import { StyleSheet } from "react-native";

export const loginStyles = StyleSheet.create({
  btn: {
    backgroundColor: "#f0f0f0",
    padding: 10,
    borderRadius: 10,
    marginVertical: 10,
  },
});
